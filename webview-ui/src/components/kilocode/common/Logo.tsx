// Logo component temporarily disabled to resolve build issues.
// TODO: Implement with correct Roxonn logo (PNG or SVG) and ensure proper asset bundling.
export default function Logo({ width = 100, height = 100 }: { width?: number; height?: number }) {
	// Props width, height are kept in signature to avoid breaking imports, but are not used.
	return null
}
